# Implementation Quality Rules

## 形骸化実装の禁止

テストを通すためだけの以下の実装は禁止:

- ハードコードされた戻り値
- 空の実装やスタブのまま放置
- 条件分岐でテストケースだけを処理

## 正しいアプローチ

1. テストが求める**汎用的な解決策**を実装する
2. 最小限だが**正しく動作する**コードを書く
3. リファクタリングで品質を向上させる

## 実装の原則

- YAGNI: 必要になるまで実装しない
- DRY: 繰り返しを避ける（ただし早すぎる抽象化も避ける）
- KISS: シンプルに保つ
