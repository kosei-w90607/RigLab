# JWT Secret (フロントエンドの AUTH_SECRET と同じ値)
NEXTAUTH_SECRET=your-secret-key-here

# フロントエンドURL（メール内リンク生成・楽天APIリファラー）
# docker-compose.yml で開発用に設定済み、本番は Render で設定
# FRONTEND_URL=http://localhost:3000

# メール送信元アドレス
# docker-compose.yml で開発用に設定済み、本番は Render で設定
# MAILER_FROM=noreply@example.com

# 楽天API
RAKUTEN_APPLICATION_ID=your_rakuten_application_id
RAKUTEN_ACCESS_KEY=your_rakuten_access_key
# RAKUTEN_ALLOWED_WEBSITE=
# 未設定時は FRONTEND_URL を使用。楽天APIのリファラー制限で別ドメインが必要な場合のみ設定

# メール送信 (Resend)
# RESEND_API_KEY=your_resend_api_key

# CORS許可ドメイン（本番のみ）
# CORS_ORIGINS=https://your-domain.com

# Sentry (optional — 未設定ならno-opで動作)
# SENTRY_DSN=

# 本番環境 (Render) — DATABASE_URL は Render が自動注入
# DATABASE_URL=postgresql://user:pass@host/dbname

# GitHub Actions 価格取得cron用シークレット
# CRON_SECRET=your-random-secret-here
